spring:
  application:
    name: viplive-lab-casus
  datasource:
    url: jdbc:h2:mem:agenda
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

server:
  port: 8080
